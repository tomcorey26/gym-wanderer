mutation Register(
  $last_name: String!
  $first_name: String!
  $birthday: String
  $password: String!
  $email: String!
  $preferences: PreferencesInput!
) {
  register(
    email: $email
    password: $password
    first_name: $first_name
    last_name: $last_name
    preferences: $preferences
    birthday: $birthday
  )
}
